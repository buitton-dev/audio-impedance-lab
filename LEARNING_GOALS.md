# Audio Impedance Lab — 教材設計方針

## 目的

この教材では、音声信号の周波数特性が送り側だけで決まるのではなく、出力インピーダンス・ケーブル・受け側入力インピーダンスの組み合わせで変化することを伝える。

スミスチャートや長い伝送線路の反射は扱わず、音声帯域の線形・集中定数モデルを使って、部品値と周波数特性の関係を直感的に確認できることを目標とする。

## 採用するモード構成

画面の並びは、興味を引きやすい具体例から共通原理、別の具体例へ展開する「案A」を採用する。

1. ギター等価回路
2. 接続とケーブル
3. エフェクター出力

「接続とケーブル」が共通原理であり、「ギター等価回路」と「エフェクター出力」がその具体例という関係にある。

## 1. ギター等価回路

### 対象

パッシブ・エレキギターをアンプへ直接接続した状態。

`Pickup → Volume / Tone → Cable → Amp input`

### 伝えたいこと

パッシブPickupは単純な信号源ではなく、大きなインダクタンスを持つ。ケーブル容量が負荷として加わることで、高域が単純に減衰するだけでなく、Pickupとの共振周波数とピークの高さが変化する。

表示するグラフは、弦・ボディ・スピーカーを含む音圧特性ではなく、Pickup内部起電力からアンプ入力までの電気的伝達特性である。

「Stratタイプ」はStratocaster系のシングルコイルPickupと250 kΩのVolume/Toneを想定し、通常3個あるPickupのうち1個だけを選択した状態を表す。

### 最初に試してほしい操作

**Cable lengthを3 mから10 mへ変更する。**

- 観察点：ケーブル合計容量の増加に伴い、共振ピークが低周波側へ移動する。
- 結論：ケーブルは単純な高域カット部品ではなく、PickupのLに対する容量性負荷として共振特性そのものを変える。

## 2. 接続とケーブル

### 対象

特定の楽器や機器に限定しない、一般化した機器間接続。

`Source Rout → Cable R/L/C → Load Rin`

### 伝えたいこと

ロー出し・ハイ受けは、送り側の電圧を次段へできるだけ変化させずに渡すための基本条件である。RoutとRinは抵抗分圧を作り、Routとケーブル容量の組み合わせは高域特性に影響する。

ここでいうHigh/Lowは信号レベルではなく、インピーダンスの大小を意味する。Routは送り側を外部から見たThevenin等価出力抵抗であり、内部回路のすべての部品を表すものではない。

Cable Lのコイル記号のループ数は、合計インダクタンスの大小を直感的に示す教材表示である。実際のケーブルが表示どおりに巻かれていることを意味しない。

### 最初に試してほしい操作

**「ロー出し・ハイ受け」プリセットのまま、Source Routを100 Ωから100 kΩへ変更する。**

- 観察点：抵抗分圧による全体レベルの低下と、ケーブル容量による高域側の変化を確認する。
- 結論：RinをRoutより十分大きくすることで、電圧を保ったまま信号を次段へ渡しやすくなる。
- 注意：Rinを100 kΩに固定してRoutだけを変更するため、送り側インピーダンスの影響を分離して観察できる。

## 3. エフェクター出力

### 対象

エフェクター出力段から、ケーブルを介して次のエフェクターまたはアンプへ接続する状態。

`内部信号源 → Rout → Cout → 出力端子 → Cable → 次段Rin`

Cout後段の出力端子からRpull-downを対地に接続する。

### 伝えたいこと

Coutは内部回路のDC電圧を次段へ流さないための出力カップリングコンデンサである。Rpull-downはCout後段の出力端子をDC的にGNDへ固定し、接続時のポップノイズを抑える役割を持つ。

Routは信号源に直列に入る出力抵抗、Rpull-downは出力端子からGNDへ入る抵抗であり、接続位置も役割も異なる。Coutは概ね`Rout + (Rpull-down || Rin)`と組み合わさって低域ハイパス特性を作る。高域側は主にRoutとケーブル容量の組み合わせで変化する。

### 最初に試してほしい操作

**Output coupling Coutを1 µFから100 nFへ変更する。**

- 観察点：低域遮断周波数の概算が約1.7 Hzから約17 Hzへ上がり、20 Hz付近の減衰が見えるようになる。
- 結論：CoutはDC遮断だけでなく、出力対地抵抗と次段Rinを含む負荷条件によって低域特性を変える。

## 操作方法（実装済み）

回路図上の部品値を直接操作する方式を主操作として採用する。常時表示する操作は、原則として「プリセット」「まず試す」「初期値に戻す」の3種類に絞る。

現在のスライダーは削除せず、「詳細設定」の折りたたみ内へ移動し、正確な設定と操作上の代替手段として残す。

### 基本操作

| 対象 | 操作 | 結果 |
|---|---|---|
| 回路図中のR・L・Cの値 | 値を上へドラッグ | 値が増加する |
| 回路図中のR・L・Cの値 | 値を下へドラッグ | 値が減少する |
| Volume／Toneノブ | ノブを上下ドラッグ | ノブ位置が変化する |
| ケーブル長 | ケーブルまたは長さ表示を上下ドラッグ | 長さと合計R/L/Cが変化する |
| 部品値 | クリックまたはタップ | 数値入力欄を開く |
| 全体 | 「初期値に戻す」 | 選択中プリセットの初期値へ戻る |

ドラッグ中は、対象部品、表示値、周波数特性をリアルタイムに更新する。選択中プリセットの初期特性を破線、変更中および変更後の特性を実線で表示し、因果関係を比較できるようにする。プリセットを変更したときだけ破線の基準も更新する。

### 値の変化規則

- 抵抗・容量・インダクタンスは対数変化とする。基準は上下120 pxのドラッグで約10倍／10分の1とする。
- ケーブル長と0～10のノブ位置は線形変化とする。
- 初期実装では既存の部品別step値へ丸め、数値入力では任意値を指定できる。E12/E24系列へのスナップは将来拡張とする。
- PCではShiftキーを押しながらドラッグすると、通常の1/5の感度で微調整できるようにする。
- 設定可能範囲を超えた場合は最小値または最大値で止める。

### 「まず試す」の導線

各モード上部に、変更対象、開始値、目標値、観察点を1行で示す。

| モード | 表示する案内 |
|---|---|
| ギター等価回路 | Cable lengthの値を上へドラッグして、3 mから10 mにする |
| 接続とケーブル | Source Routの値を上へドラッグして、100 Ωから100 kΩにする |
| エフェクター出力 | Coutの値を下へドラッグして、1 µFから100 nFにする |

案内には「場所を示す」操作を設け、押すと対応する回路部品と値表示を強調する。値を自動変更することは基本動作にせず、利用者自身がドラッグして変化を確認する構成とする。

操作が目標値付近へ到達したら、強調表示を完了状態へ変え、グラフのどこを見るべきかを短く示す。「元に戻す」で同じ比較を繰り返せるようにする。

### スマートフォンとアクセシビリティ

- 部品値のドラッグ対象だけで画面スクロールを一時的に抑止し、それ以外の場所では通常どおり縦スクロールできるようにする。
- ドラッグ対象には十分なタッチ領域を持たせ、値そのものの文字サイズは維持する。
- タップによる数値入力と「詳細設定」のスライダーを、ドラッグ操作の代替手段として残す。
- キーボードでは対象値へフォーカスし、上下矢印キーで増減できるようにする。
- 色だけに依存せず、枠、背景、ラベルでも操作対象と完了状態を示す。

### 採用理由

スライダーだけの構成は画面を占有し、回路図中の部品との対応を追いにくい。ドラッグ操作だけでは正確な値の設定とスマートフォン操作に弱点がある。そのため、回路図上の直接操作を主、折りたたみ式スライダーと数値入力を補助とするハイブリッド方式を採用する。

## モデルの範囲

- 受動・線形・集中定数モデルを基本とする。
- 長い伝送線路の反射、特性インピーダンス、スミスチャートは扱わない。
- ギターの弦・ボディ・ピッキング位置、スピーカー、室内音響は含まない。
- エフェクターのクリッピング、能動回路固有の帯域、電源条件、非線形特性は含まない。
- 絶対的な「音色」の再現ではなく、回路定数と負荷による相対的な変化の理解を目的とする。
