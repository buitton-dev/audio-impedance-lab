# Audio Impedance Lab — 教材設計方針

## 目的

この教材では、音声信号の周波数特性が送り側だけで決まるのではなく、出力インピーダンス・ケーブル・受け側入力インピーダンスの組み合わせで変化することを伝える。

スミスチャートや長い伝送線路の反射は扱わず、音声帯域の線形・集中定数モデルを使って、部品値と周波数特性の関係を直感的に確認できることを目標とする。

## 採用するモード構成

画面の並びは、興味を引きやすい具体例から共通原理、別の具体例へ展開する「案A」を採用する。

1. ギター等価回路
2. 接続とケーブル
3. エフェクター出力

「接続とケーブル」が共通原理であり、「ギター等価回路」と「エフェクター出力」がその具体例という関係にある。

## 1. ギター等価回路

### 対象

パッシブ・エレキギターをアンプへ直接接続した状態。

`Pickup → Volume / Tone → Cable → Amp input`

### 伝えたいこと

パッシブPickupは単純な信号源ではなく、大きなインダクタンスを持つ。ケーブル容量が負荷として加わることで、高域が単純に減衰するだけでなく、Pickupとの共振周波数とピークの高さが変化する。

表示するグラフは、弦・ボディ・スピーカーを含む音圧特性ではなく、Pickup内部起電力からアンプ入力までの電気的伝達特性である。

「Stratタイプ」はStratocaster系のシングルコイルPickupと250 kΩのVolume/Toneを想定し、通常3個あるPickupのうち1個だけを選択した状態を表す。

## 2. 接続とケーブル

### 対象

特定の楽器や機器に限定しない、一般化した機器間接続。

`Source Rout → Cable R/L/C → Load Rin`

### 伝えたいこと

ロー出し・ハイ受けは、送り側の電圧を次段へできるだけ変化させずに渡すための基本条件である。RoutとRinは抵抗分圧を作り、Routとケーブル容量の組み合わせは高域特性に影響する。

ここでいうHigh/Lowは信号レベルではなく、インピーダンスの大小を意味する。Routは送り側を外部から見たThevenin等価出力抵抗であり、内部回路のすべての部品を表すものではない。

Cable Lのコイル記号のループ数は、合計インダクタンスの大小を直感的に示す教材表示である。実際のケーブルが表示どおりに巻かれていることを意味しない。

## 3. エフェクター出力

### 対象

エフェクター出力段から、ケーブルを介して次のエフェクターまたはアンプへ接続する状態。

`内部信号源 → Cout → 出力直列Rout → 出力端子 → Cable → 次段Rin`

Cout後のRoutを通過した出力端子からRpull-downを対地に接続する。CoutとRoutの間は、将来Volume potを対地接続できる節点として扱う。

### 伝えたいこと

Coutは内部回路のDC電圧を次段へ流さないための出力カップリングコンデンサである。Rpull-downはCout後段の出力端子をDC的にGNDへ固定し、接続時のポップノイズを抑える役割を持つ。

RoutはCout後に実在する直列出力抵抗、Rpull-downは出力端子からGNDへ入る抵抗であり、接続位置も役割も異なる。Coutは概ね`Rout + (Rpull-down || Rin)`と組み合わさって低域ハイパス特性を作る。高域側は主にRoutとケーブル容量の組み合わせで変化する。現在はCoutとRoutの間に分岐がないので直列順序による計算差はないが、将来Volume potを追加するときは、その節点を含む回路として再計算する。


## PC／スマホ別の画面構成

画面幅700 px以下をスマホ表示、それより広い画面をPC表示とする。画面幅による切替で設定値・プリセット・計算モデルは変えない。

PCでは従来の回路図中心の表示と詳細設定スライダーを維持する。スマホでは長い回路図の横スクロールを主操作にせず、「部品を操作」パネルで操作対象を選ぶ。グラフをパネル上側に固定し、値を動かしたときの変化を確認しやすくする。高さ600 px以下ではグラフの固定を解除する。

| スマホの操作場所 | 対象 |
|---|---|
| ギター：Pickup | DCR、L、自己容量、減衰抵抗 |
| ギター：Volume / Tone | ポット総抵抗値、ノブ位置、Tone C |
| ギター：アンプ | Rin、Grid stopper、実効入力C |
| 接続とケーブル：接続機器 | Rout、Rin |
| エフェクター：出力回路 | Cout、直列Rout、Rpull-down、次段Rin |
| 各モード：ケーブル | 長さと単位長定数（ギターはCのみ） |

各部品に短辺48 px以上の＋／−ボタンと、高さ56 px以上の記号・値ボタンを用意する。＋／−はR/L/Cなら系列上の1段階、長さなら0.5 m、ノブ位置なら0.1ずつ変更する。中央ボタンは上下ドラッグとタップ数値入力に対応する。操作中にボタン全体を作り直さず、連続操作とフォーカスを維持する。

スマホでは詳細設定スライダーを隠し、全設定を部品操作パネルから操作する。全体回路図は開閉ボタンで展開でき、回路図と実物の対応、接続関係を確認できる。PCとスマホの表示は共通の状態更新処理へ接続する。

## 操作方法（実装済み）

回路図上の数値とR・L・Cシンボルを直接操作する方式を主操作として採用する。常時表示する操作は、原則として「プリセット」と「初期値に戻す」に絞る。

PCではスライダーを「詳細設定」の折りたたみ内に残す。スマホでは部品操作パネルの＋／−と数値入力を代替手段とする。

### 基本操作

| 対象 | 操作 | 結果 |
|---|---|---|
| 通常のR・L・Cの数値またはシンボル | 上／下へドラッグ | 対応する部品値が増加／減少する |
| Volume／Toneの抵抗シンボル | 上下ドラッグ | ポットの総抵抗値が変化する |
| Volume knob／Tone knobの数値 | 上下ドラッグ | ノブ位置が変化する |
| Cable lengthの数値 | 上下ドラッグ | 長さと合計R/L/Cが変化する（ギターはCのみ） |
| ケーブル内のR・L・Cシンボル | 上下ドラッグ | 単位長定数が変わり、合計値へ反映される |
| 数値またはR・L・Cシンボル | クリックまたはタップ | 対応する数値入力欄を開く |
| 全体 | 「初期値に戻す」 | 選択中プリセットの初期値へ戻る |

ドラッグ中は、対象部品、表示値、周波数特性をリアルタイムに更新する。選択中プリセットの初期特性を破線、変更中および変更後の特性を実線で表示し、因果関係を比較できるようにする。プリセットを変更したときだけ破線の基準も更新する。

### 値の変化規則

- 抵抗・容量・インダクタンスはE24系列を基本とする離散値で変化させる。250/500 kΩのギターポット、34 kΩ、Pickup公称値、ケーブルの単位長実測値などは用途固有値として候補へ追加する。
- ケーブル長と0～10のノブ位置は線形変化とする。
- ドラッグ、詳細設定、キーボード、数値入力のいずれも、最も近い候補値へスナップする。
- PCではShiftキーを押しながらドラッグすると、候補値の移動感度を下げる。
- 設定可能範囲を超えた場合は最小値または最大値で止める。

### 設定範囲と極端値での実験

| 対象 | 設定範囲 | 値の選び方 |
|---|---|---|
| ケーブル以外のR | 1 Ω〜100 MΩ | E24系列＋用途固有値。Grid stopperは0 Ωも可 |
| Pickup L | 1 µH〜100 H | E24系列＋Pickup公称値など |
| ケーブル以外のC | 1 pF〜1,000 µF | E24系列＋用途固有値 |
| ケーブル長（全3モード） | 0.5〜100 m | 0.5 m刻み |
| Volume／Toneノブ位置 | 0〜10 | 通常0.1刻み |

ケーブルの単位長定数の範囲は従来どおり、Rは0〜1 Ω/m、Lは0〜2 µH/m、Cは50〜200 pF/mです。ギターモードではケーブル容量のみを扱います。プリセットの初期値は変更していません。

現実的な値の周辺だけでなく、共振や減衰が大きく変化する極端値も許容する。これは理想化モデルの因果関係を観察するための範囲であり、実部品の入手性、実機の成立、寄生成分を含む精度を保証するものではない。

周波数軸は20 Hz〜20 kHzの対数表示を維持する。利得の縦軸は現在値と初期値の両曲線を含むよう自動調整し、極端値の曲線が固定範囲外に張り付くのを防ぐ。表示下限処理は−240 dB。目盛りが変わるため、比較時は曲線形状とdB値の両方を見る。

### スマートフォンとアクセシビリティ

- 数値とシンボルの操作領域だけで画面スクロールを抑止し、余白では通常の縦スクロールと回路図の横スクロールを可能にする。
- シンボル本体に透明な操作領域を重ねる。最小表示時でも短辺約45 pxを確保し、細い線を正確になぞらなくても操作できるようにする。
- スマホでは＋／−とタップ数値入力をドラッグの代替にする。PCでは「詳細設定」のスライダーも使用できる。
- キーボードでは数値またはシンボルへフォーカスし、上下矢印キーで増減、Enter／Spaceで数値入力を開けるようにする。
- 色だけに依存せず、枠、背景、ラベルでも操作対象を示す。

### 採用理由

スライダーだけの構成は画面を占有し、回路図中の部品との対応を追いにくい。ドラッグ操作だけでは正確な値の設定とスマートフォン操作に弱点がある。そのため、回路図上の直接操作を主、折りたたみ式スライダーと数値入力を補助とするハイブリッド方式を採用する。

## モデルの範囲

- 受動・線形・集中定数モデルを基本とする。
- 長い伝送線路の反射・遅延、特性インピーダンス、スミスチャートは扱わない。ケーブル長を100 mにしても集中定数モデルを維持する。
- ギターの弦・ボディ・ピッキング位置、スピーカー、室内音響は含まない。
- エフェクターのクリッピング、能動回路固有の帯域、電源条件、非線形特性は含まない。
- 絶対的な「音色」の再現ではなく、回路定数と負荷による相対的な変化の理解を目的とする。
